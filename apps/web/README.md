# @webxr-pool/web

SvelteKit shell plus the Babylon.js game session. Built with the static adapter; the output in `build/` is served by `apps/server`.

Run from the repository root: `pnpm dev`. Rules for this app: [CLAUDE.md](./CLAUDE.md).
