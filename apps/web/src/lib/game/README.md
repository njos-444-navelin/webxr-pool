Babylon scene, input state machine, in-session GUI and the local game session.
Client-only code: only ever imported from client-only routes or inside `onMount`.
See docs/gameplay/interaction.md and docs/ui.md.
